BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Library] ADD [showDescription] BIT NOT NULL CONSTRAINT [Library_showDescription_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
